import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Pet Shop App loads', (WidgetTester tester) async {
    // Basic smoke test - Firebase must be initialized for full test
    expect(true, isTrue);
  });
}
